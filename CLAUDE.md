# Groot AI Agent 项目规范

## 目录结构规范

项目采用以下标准目录结构，**所有新增文件必须遵循此规范**：

```
groot/
├── cmd/                    # 程序入口（只放 main.go）
├── internal/               # 内部模块（核心源码，不对外暴露）
├── pkg/                    # 公共工具包（可被外部项目引用）
├── docs/                   # 所有文档（设计文档、用户手册、API规范等）
├── tests/                  # 测试目录（测试相关所有内容）
│   ├── python/             # Python 测试程序
│   ├── logs/               # 测试运行日志
│   ├── temp/               # 测试临时文件
│   ├── reports/            # 测试报告
│   └ fixtures/             # 测试数据
├── bin/                    # 编译输出（.gitignore 忽略内容）
```

### 规则

1. **源码目录**: `cmd/`、`internal/`、`pkg/` - 只放 Go 源码
2. **文档目录**: `docs/` - 所有文档必须放在这里，不要在根目录或其他地方创建文档
3. **测试目录**: `tests/` - 测试程序、测试产物、临时文件、测试报告都放在这里
4. **编译输出**: `bin/` - 编译产物放在这里，目录保留但内容被 gitignore

### 禁止的行为

- ❌ 不要在根目录创建临时测试目录（如 `test_xxx/`、`temp_xxx/`）
- ❌ 不要在项目根目录创建 `logs/` 目录
- ❌ 不要在 `docs/` 外创建文档文件
- ❌ 不要创建奇怪的目录名（如 `~/`）

## 代码规范

- Go 代码遵循标准 Go 风格
- 使用 `gofmt` 格式化代码
- 测试使用 Python（pytest），放在 `tests/python/`

## 配置说明

- 运行时配置自动生成到 `~/.groot/config.yaml`
- 不要在项目目录创建 `configs/` 目录