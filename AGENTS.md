# Groot AI Agent 项目规范

## 目录结构规范

项目采用以下标准目录结构，**所有新增文件必须遵循此规范**：

```
groot/
├── cmd/                    # 程序入口（只放 main.go）
├── internal/               # 内部模块（核心源码，不对外暴露）
├── pkg/                    # 公共工具包（可被外部项目引用）
├── docs/                   # 设计文档、开发计划等（不含用户手册）
├── examples/               # 客户端代码示例（按语言分子目录）
│   ├── python/             # Python 示例
│   └── java/               # Java 示例（Maven 项目）
├── tests/                  # 测试目录（测试相关所有内容）
│   ├── python/             # Python 系统测试
│   ├── examples/           # 示例代码的测试
│   │   ├── python/         # Python 示例测试
│   │   └── java/           # Java 示例测试
│   ├── logs/               # 测试运行日志
│   ├── temp/               # 测试临时文件
│   ├── reports/            # 测试报告（含测试说明 README.md）
│   └ fixtures/             # 测试数据
├── dist/                   # 编译输出（三平台 zip 发布产物入库，其余被 gitignore）
├── README.md               # 用户使用手册（GitHub 首页展示）
```

### 规则

1. **源码目录**: `cmd/`、`internal/`、`pkg/` - 只放 Go 源码
2. **用户手册**: `README.md`（根目录） - 用户使用说明，**每次更新使用手册必须修改此文件**
3. **设计文档**: `docs/` - 设计文档、开发计划等内部文档
4. **示例代码**: `examples/` - 客户端代码示例，按语言分子目录（`python/`、`java/` 等）
5. **测试目录**: `tests/` - 所有测试代码和产物
   - 示例的测试代码放在 `tests/examples/<语言>/`，**不允许放在 examples/ 目录下**
   - 对于 Maven/Java 项目，需在 `pom.xml` 中配置 `<testSourceDirectory>` 指向 `tests/examples/java/`
6. **编译输出**: `dist/` - 编译产物放在这里（单平台为 `dist/groot`，`make build-all` 产出三个平台的 zip 作为发布产物提交到仓库；`dist/groot` 及临时目录被 gitignore）

### 禁止的行为

- ❌ 不要在根目录创建临时测试目录（如 `test_xxx/`、`temp_xxx/`）
- ❌ 不要在项目根目录创建 `logs/` 目录
- ❌ 不要创建 `docs/user-guide.md` - 用户手册就是根目录的 `README.md`
- ❌ 不要创建奇怪的目录名（如 `~/`）
- ❌ 不要在 `examples/<语言>/` 目录下创建测试文件 - 测试一律放 `tests/examples/<语言>/`

## 开发工作流程

实施 plan 后的代码开发，遵循以下流程：

```
Plan → 编写/更新设计文档 → 编写代码 → 编写单元测试 → 运行单元测试 → 提交
```

### 流程说明

1. **编写/更新设计文档**: 新增或修改功能时，必须在 `docs/superpowers/specs/` 下创建或更新对应的设计文档
2. **编写代码**: 按 plan 实现功能
3. **编写单元测试**: 为新增/修改的 `internal/` 包代码编写 Go 单元测试
4. **运行单元测试**: `go test ./internal/xxx/... -v`，确保全部通过
5. **提交**: 用户确认后提交代码和测试

### 职责分工

| 测试类型 | 执行者 | 说明 |
|---------|-------|------|
| 单元测试（Go） | Claude | 代码完成后自动编写并运行 |
| 系统测试（Python） | 用户 | 用户自行运行 `tests/python/` 下的测试 |

### 单元测试要求

- 每个新增的 `internal/` 包功能必须配套单元测试
- 测试覆盖：正常流程、边界条件、错误处理
- 测试通过后才能提交代码

## 设计文档输出结构规范

在编写功能设计文档时，**必须严格遵守以下输出结构，两部分内容禁止交叉**：

### 1. 功能设计部分

- 只描述这个功能本身**是什么**、**具备哪些能力**、**解决什么问题**
- 用**正面陈述**的方式，描述功能的完整形态
- 就像第一次设计这个功能一样，**独立完整地表述**
- ❌ 禁止出现以下对比性语言：
  - "相比之前……"
  - "与旧版不同……"
  - "新增了……"
  - "保留了……"
  - "原来是 X，现在改为 Y……"
  - 其他任何隐含"对比上一版"的措辞

### 2. 迭代说明部分（另起章节）

- **另起独立章节**，与功能设计部分明确分隔
- 在这里才说明与上一版的**差异、改动点、新增/删除/调整**的内容
- 可以使用对比性语言（"新增"、"移除"、"调整为"、"相比 vX 版本"等）

### 文档骨架示例

```markdown
# 功能名称设计文档

## 一、功能设计

### 1.1 功能概述
（这个功能是什么、解决什么问题，独立完整表述）

### 1.2 能力清单
（具备哪些能力，正面陈述）

### 1.3 设计细节
（架构、接口、数据模型等，正面陈述）

## 二、迭代说明

### 2.1 与上一版差异
- 新增：……
- 移除：……
- 调整：……
```

### 适用范围

- `docs/superpowers/specs/` 下的所有设计文档
- 其他任何描述功能设计的文档（含 README 中的功能设计章节）

## 测试规范

项目采用分层测试策略，**禁止引入新的测试模式或工具**：

### 单元测试（Go）

- **测试对象**: `internal/` 包内的单个函数、结构体、逻辑单元
- **测试工具**: Go 标准测试框架 (`*_test.go`)
- **目录位置**: 与源码同目录，如 `internal/mcp/xxx_test.go`
- **运行命令**: `go test ./internal/xxx/... -v`
- **命名规范**: `Test<功能名>` 或 `Test<结构体>_<方法名>`
- **执行时机**: 代码完成后立即编写并运行

### 系统测试（Python）

- **测试对象**: API 端点、CLI 命令、完整流程、端到端场景
- **测试工具**: Python pytest
- **目录位置**: `tests/python/`
- **运行命令**: `cd tests/python && pytest -v`
- **执行时机**: 用户自行决定何时运行

### 测试目录结构

```
tests/
├── python/             # Python 系统测试（pytest）
├── examples/           # 示例代码的测试
│   ├── python/         # Python 示例测试
│   └── java/           # Java 示例测试
├── fixtures/           # 测试数据文件
├── logs/               # 测试运行日志
├── temp/               # 测试临时文件
├── reports/            # 测试报告（含 README.md）
└── abs_path_test/      # 绝对路径测试环境
```

### 测试用例汇总文档

所有测试用例点汇总保存在: `tests/TEST_CASES.md`

### 禁止的行为

- ❌ 禁止使用其他测试工具（如 Jest、Ruby 等）
- ❌ 禁止创建新的测试目录结构
- ❌ 禁止在根目录创建测试文件
- ❌ 禁止用 Python 测试 internal 包的单元逻辑

## 代码规范

- Go 代码遵循标准 Go 风格
- 使用 `gofmt` 格式化代码

## 配置说明

- 运行时配置自动生成到 `~/.groot/config.yaml`
- 不要在项目目录创建 `configs/` 目录

## Git 提交规范

- **所有 git commit 必须由用户明确请求**，禁止自动提交
- 只有用户明确说"提交"、"commit"、"推送到远程"等指令时，才执行 git 操作

## 编译规范

- 编译产物必须输出到 `dist/` 目录
- 编译命令：`go build -o dist/groot ./cmd`
- 不要在项目根目录留下编译产物